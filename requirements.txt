# TamAGI Dependencies
# Core
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
pyyaml>=6.0.2
httpx>=0.27.0

# Memory / Vector DB
chromadb>=0.5.0

# Elasticsearch memory backend (optional — only needed when memory.backend = "elasticsearch")
elasticsearch[async]>=8.0.0
sentence-transformers>=3.0.0

# Web Search (free provider — no API key needed)
ddgs>=7.0.0

# Self-model graph
networkx>=3.0

# MCP (Model Context Protocol) — tool server integration
mcp>=1.0.0

# Secret storage — OS keyring primary, Fernet-encrypted file fallback
keyring>=25.0.0
cryptography>=42.0.0

# Cron scheduling for world thread
croniter>=2.0

# Utilities
python-multipart>=0.0.12
aiofiles>=24.1.0
jinja2>=3.1.4
itsdangerous>=2.2.0
