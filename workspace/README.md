# TamAGI Workspace

This is TamAGI's default working directory. Files placed here are accessible
to TamAGI's `read` and `write` skills.

You can:
- Drop files here for TamAGI to analyze
- Let TamAGI create files here
- Use this as a shared working space

The workspace path is configurable in `config.yaml`.
